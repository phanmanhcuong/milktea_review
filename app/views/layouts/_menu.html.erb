<div class="collapse navbar-collapse">
  <ul class="nav navbar-nav navbar-right">
    <% if user_signed_in? %>
      <li><%= link_to t(".write_review"), new_review_path %></li>
      <div class="dropdown">
        <button class="dropbtn"><%= t "navbar.account" %></button>
        <div class="dropdown-content">
          <li><%= link_to t(".My Profile"), user_path(current_user) %></li>
          <li><%= link_to t(".setting"), edit_user_registration_path %></li>
          <li class="divider"></li>
          <li>
            <%= link_to t(".logout"), destroy_user_session_path, method: :delete %>
          </li>
          <li class="divider"></li>
        </div>
      </div>
    <% else %>
      <li><%= link_to t("navbar.login"), new_user_session_path %></li>
      <li id="signup"><%= link_to t("navbar.signup"), new_user_registration_path %></li>
    <% end %>
  </ul>
</div>
